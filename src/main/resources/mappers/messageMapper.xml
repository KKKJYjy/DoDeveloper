<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dodeveloper.mappers.messageMapper">

	<select id="selectMessageByNo" resultType="MessageVO">
		select * from message where messageNo = ${messageNo}
	</select>

	<select id="selectMessagesByUserId" resultType="MessageVO">
		select * from message where writer = #{userId} order by messageNo desc limit #{startPoint}, #{amountToShow};
	</select>

	<insert id="insertIntoMessage" useGeneratedKeys="true" keyProperty="messageNo" parameterType="MessageDTO">
		insert into message(writer, title, content) values(#{writer}, #{title}, #{content});
	</insert>
</mapper>